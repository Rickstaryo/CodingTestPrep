#include <bits/stdc++.h>
using namespace std;
int n, m, j, l, r, temp, ret;
int main()
{
    cin >> m >> n >> j;
    l = 1;
    for (int i = 0; i < m; i++)
    {
        r = l + m - 1;
        cin >> temp;
        if (temp >= l && temp <= r)
            continue;
        else
        {
            if (temp < l)
            {
                ret += (l - te, [])
            }
        }
    }
}